# TODO: Fix react-datepicker CSS import

- [ ] Edit `sec-nexus/app/globals.css` to add `import "react-datepicker/dist/react-datepicker.css";` after the existing imports
- [ ] Run `npm run dev` to start the development server
- [ ] Test the EventForm component to ensure datepicker styling renders correctly
- [ ] Remove the import from `EventForm.tsx` if necessary (after confirming global import works)
